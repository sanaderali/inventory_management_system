# Subdomain .htaccess for inventory.techsolutionspro.co.uk

# Enable URL rewriting
RewriteEngine On

# Set the base directory
RewriteBase /

# Redirect www to non-www for the subdomain
RewriteCond %{HTTP_HOST} ^www\.inventory\.techsolutionspro\.co\.uk [NC]
RewriteRule ^(.*)$ http://inventory.techsolutionspro.co.uk/$1 [L,R=301]

# Redirect HTTP to HTTPS for the subdomain
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Rest of your rules and configurations go here

# End of subdomain .htaccess
    # testsub